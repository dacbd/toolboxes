name: Build & Push Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build_push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install just
        run: |
          sudo apt-get update
          sudo apt-get install -y just

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Install Trivy
        run: just setup

      - name: Build agents for scanning (amd64)
        run: just agents build-amd64

      - name: Scan agents (amd64)
        run: just agents scan agents:local-amd64

      - name: Build agents for scanning (arm64)
        run: just agents build-arm64

      - name: Scan agents (arm64)
        run: just agents scan agents:local-arm64

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push debug
        run: just debug push

      - name: Build & Push agents
        run: just agents push
